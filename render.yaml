services:
  - type: web
    name: restaurant-qr-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: HF_REPO
        value: YOUR_HF_USERNAME/indian-food-recommender
